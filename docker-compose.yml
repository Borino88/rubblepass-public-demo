version: '3.8'

services:
  rubblepass-api:
    build: .
    container_name: rubblepass_demo_api
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=development
      - PORT=8000
    volumes:
      - .:/app
    restart: unless-stopped
